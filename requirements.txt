# Weather Diary - Requirements
# Приложение использует стандартные библиотеки Python
# Никаких дополнительных зависимостей не требуется

# Стандартные библиотеки (устанавливаются с Python):
# - tkinter (для GUI)
# - json (для работы с данными)
# - datetime (для валидации дат)
# - os (для работы с файловой системой)
# - unittest (для тестирования)